<UserControl x:Class="EditorWPF.TabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EditorWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <DockPanel.Background>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ActiveCaptionColorKey}}"/>
            </DockPanel.Background>
            <Label x:Name="name" Content="Name" Foreground="White" Margin="0,-5,0,0"/>
            <Button HorizontalAlignment="Right" Background="Transparent" BorderBrush="#00707070">
                <Button.Content>
                    <Image Source="/Resources/Close.png"  />
                </Button.Content>
            </Button>

        </DockPanel>




    </Grid>
</UserControl>
